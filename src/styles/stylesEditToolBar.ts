import { makeStyles } from '@material-ui/core'

export const useStylesEditToolBar = makeStyles((theme) => ({
  button: {
    width: 48,
    height: 48,
    margin: 0,
  },
  toolBar: {
    padding: 0
  }
}))
